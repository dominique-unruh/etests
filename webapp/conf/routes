# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /preview/:assessment/              controllers.AssessmentController.assessment(assessment)
GET     /preview/:assessment/:file         controllers.AssessmentController.assessmentFile(assessment, file)
GET     /answers/:assessment/:regno         controllers.AssessmentController.loadAnswers(assessment, regno)
GET     /reference/:assessment         controllers.AssessmentController.loadReference(assessment)
POST    /response/:assessment/update controllers.AssessmentController.updateAction(assessment)
GET     /routes.js                         controllers.MiscController.javascriptRoutes

GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)